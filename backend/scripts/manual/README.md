# backend/scripts/manual/

Scripts de verificación manual contra una DB (conectan a host/puerto hardcodeado).
NO son tests de pytest — se sacaron de `backend/tests/` porque rompían la colección.
Correr a mano cuando se necesite, ajustando la conexión.
